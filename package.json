{
  "name": "github-script",
  "version": "0.4.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts",
    "precommit": "npm run build && git add dist/"
  },
  "author": "GitHub",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.1.0",
    "@actions/github": "^1.1.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  }
}
